/*
) سؤال
أكتب برنامج يطلب من المستخدم رقمًا ثم احسب مجموع الأرقام الفردية فقط من 1 إلى الرقم المدخل.
 لكن داخل كل دورة loop افحص إذا كان الرقم أكبر من 100 وإذا كان كذلك أوقفي البرنامج فورًا.
 */
#include <iostream>
using namespace std;

int main() {
    int N;
    cout << "أدخل رقمًا: ";
    cin >> N;

    int sum = 0;
    for (int i = 1; i <= N; i++) {
        if (i > 100) {
            cout << "الرقم تجاوز 100. التوقف عن الحساب." << endl;
            return 0;
        }
        if (i % 2 != 0) {
            sum += i;
        }
    }

    cout << "مجموع الأرقام الفردية من 1 إلى " << N << " هو: " << sum << endl;
    return 0;
}